{
  "name": "whatsapp-webhook-local",
  "version": "1.0.0",
  "description": "Local WhatsApp webhook server for Cal AI",
  "main": "server.js",
  "scripts": {
    "start": "node server-simple.js",
    "start:api": "node api-server.js",
    "start:both": "concurrently \"npm run start\" \"npm run start:api\"",
    "dev": "nodemon server-simple.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0",
    "twilio": "^5.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1"
  }
}
